# 绣绣助手（本地离线版）

这个项目已移除镜像代理能力，仅保留本地前端页面。

## 启动

```bash
npm install
npm run dev
```

访问地址：`http://127.0.0.1:5420/`

## 构建

```bash
npm run build
```

## 说明

- 不依赖外部站点，不需要登录，不会同步线上账号数据。
- 本地使用只受你电脑环境影响，账号是否过期不会导致本地页面打不开。
